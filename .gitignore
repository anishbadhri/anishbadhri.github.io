# Dependencies
node_modules/

# Build output (generated by CI on deploy)
_site/

# CV generator scratch output (.tex intermediate + local PDF compile)
build/

# Generated résumé PDF — CI generates and publishes this on deploy, so it is
# not committed. Run `npm run cv` for a local copy (needs a local TeX install).
src/assets/anish-badri-resume.pdf

# Claude Code local project settings
.claude/

# OS / editor noise
.DS_Store
*.log
.cache/
