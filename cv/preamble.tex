%% ---------------------------------------------------------------------------
%% CV design — HAND-AUTHORED. This is the Awesome-CV class configuration: the
%% design knobs (paper, margins, accent colour, section style). It is NOT
%% machine-generated. cv/generate.js appends the personal-info commands and the
%% section content (both derived from data/) after this file, then opens the
%% document and closes it.
%%
%% Class: Awesome-CV (vendored, unmodified, in cv/vendor/ — see its README).
%% Engine: lualatex (Unicode + fontspec; loads Source Sans 3 / Roboto /
%% FontAwesome 6 from TeX Live). The CI compile step must match — NOT pdflatex.
%% ---------------------------------------------------------------------------
\documentclass[11pt, letterpaper]{awesome-cv}

% Page margins.
\geometry{left=1.4cm, top=.8cm, right=1.4cm, bottom=1.8cm, footskip=.5cm}

% Accent colour: the site's #642ab3 (rather than an awesome-* preset).
\definecolor{awesome}{HTML}{642AB3}

% Academic icons (\aiarXiv for the arXiv project links). FontAwesome (\faGithub)
% is already provided by the awesome-cv class via fontawesome6.
\usepackage{academicons}

% Fill the section-title bar with the accent colour.
\setbool{acvSectionColorHighlight}{true}

% Separator between header social entries.
\renewcommand{\acvHeaderSocialSep}{\quad\textbar\quad}
